name: Vite Build and Deploy

on:
  # main 브랜치에 푸시가 발생할 때 이 워크플로우를 실행합니다.
  push:
    branches: [ "main", "master" ]
  # GitHub Actions 탭에서 수동 실행 가능하도록 설정합니다.
  workflow_dispatch:

# 필요한 권한을 명시하여 보안을 강화하고 배포를 가능하게 합니다.
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  # 1. 빌드 작업
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          
      - name: Install dependencies
        # 프로젝트의 모든 패키지를 다시 설치합니다.
        run: npm ci 
        
      - name: Build project (Vite)
        # 🚨 이 단계에서 'dist' 폴더가 GitHub 서버에서 새로 생성됩니다!
        run: npm run build 

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        # 빌드 결과물인 'dist' 폴더를 배포 아티팩트로 업로드합니다.
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist' 
  
  # 2. 배포 작업
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build # build 작업이 성공해야만 deploy 작업이 실행됩니다.
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4